import { AppPage } from './app.po';

/**
 * Escreva nesse script todos os testes e2e que deseja fazer
 * na aplicação. [ Utilize o script 'app.po.ts' para auxiliar nessas tarefas ]
 */
describe('business-saga-app App', () => {
  let page: AppPage;

  beforeEach(() => {
    page = new AppPage();
  });

  // it('teste title', () => { });
});
