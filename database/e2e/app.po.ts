import { browser, by, element } from 'protractor';

/**
 * Classe auxiliar para criação de métodos que retornem o conteúdo desejado.
 * Crie métodos onde exista sempre um retorno, e no 'app.e2e-spec.ts' verifique se
 * o retorno é como esperado ou não.
 */
export class AppPage { }
